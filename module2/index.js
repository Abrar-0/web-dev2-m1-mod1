// index.js

// Function to greet the user
function greetUser(name) {
    return `Hello, ${name}! Welcome to Web Development.`;
}

// Call the function and log the result
const greeting = greetUser('Abrar');
console.log(greeting);
